GitHubの認証と操作に使う以下のツールを、両方ともインストールしてください。

- `git`：バージョン管理の本体（コミット・プッシュなどに必要）
- `gh`：GitHub公式の便利ツール（認証で使用）

すでにインストール済みのものはスキップしてください。

私のOSに合わせて、適切なコマンドでインストールをお願いします。
（macOSなら brew、Windows 11なら winget）

インストール完了後、`git --version` と `gh --version` を実行して、
両方のバージョン番号が表示されることを確認してください。
